/**
 * Created by Administrator on 2016/8/4 0004.
 */

(function () {
    var str = "Hello Word!";
    console.log(str);
    // str = str.substr(0, 3);  //substr 开始截取位置，截取长度；
    // str = str.substring(0, 3);//substring 开始截取位置，截取到的位置；

   var index = str.indexOf("l");

    console.log(index);


    console.log(str);
})();